const pool = require('./database');

async function runMigrations() {
  const client = await pool.connect();
  try {
    console.log('Running database migrations...');
    await client.query('CREATE EXTENSION IF NOT EXISTS "uuid-ossp"');
    await client.query('CREATE EXTENSION IF NOT EXISTS "pgcrypto"');

    await client.query(`CREATE TABLE IF NOT EXISTS stores (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), name VARCHAR(200) NOT NULL, slug VARCHAR(100) UNIQUE NOT NULL, owner_name VARCHAR(150) NOT NULL, phone VARCHAR(15) NOT NULL UNIQUE, email VARCHAR(255), password_hash VARCHAR(255) NOT NULL, logo_url TEXT, tagline VARCHAR(300), primary_color VARCHAR(7) DEFAULT '#1B4F72', address TEXT NOT NULL, city VARCHAR(100) NOT NULL, state VARCHAR(100) NOT NULL, pincode VARCHAR(6) NOT NULL, latitude DECIMAL(10,7), longitude DECIMAL(10,7), license_number VARCHAR(50), gst_number VARCHAR(20), delivery_radius_km DECIMAL(5,2) DEFAULT 5.00, delivery_charge DECIMAL(8,2) DEFAULT 0.00, min_order_amount DECIMAL(8,2) DEFAULT 0.00, is_delivery_enabled BOOLEAN DEFAULT true, is_pickup_enabled BOOLEAN DEFAULT true, operating_hours JSONB DEFAULT '{}', plan VARCHAR(20) DEFAULT 'free' CHECK (plan IN ('free','basic','pro','enterprise')), plan_expires_at TIMESTAMP WITH TIME ZONE, max_patients INTEGER DEFAULT 50, is_active BOOLEAN DEFAULT true, is_verified BOOLEAN DEFAULT false, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS patients (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), phone VARCHAR(15) NOT NULL UNIQUE, name VARCHAR(150), email VARCHAR(255), password_hash VARCHAR(255), date_of_birth DATE, gender VARCHAR(10) CHECK (gender IN ('male','female','other')), blood_group VARCHAR(5), allergies TEXT[], conditions TEXT[], address TEXT, city VARCHAR(100), pincode VARCHAR(6), latitude DECIMAL(10,7), longitude DECIMAL(10,7), is_active BOOLEAN DEFAULT true, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS store_patients (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE, patient_id UUID NOT NULL REFERENCES patients(id) ON DELETE CASCADE, loyalty_points INTEGER DEFAULT 0, lifetime_value DECIMAL(12,2) DEFAULT 0.00, total_orders INTEGER DEFAULT 0, adherence_score DECIMAL(5,2) DEFAULT 0.00, risk_level VARCHAR(20) DEFAULT 'normal' CHECK (risk_level IN ('normal','at_risk','lapsed')), last_order_at TIMESTAMP WITH TIME ZONE, family_group_id UUID, relationship VARCHAR(50), tags TEXT[], notes TEXT, joined_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), is_active BOOLEAN DEFAULT true, UNIQUE(store_id, patient_id))`);

    await client.query(`CREATE TABLE IF NOT EXISTS medicines (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), name VARCHAR(300) NOT NULL, generic_name VARCHAR(300), manufacturer VARCHAR(200), category VARCHAR(100), composition TEXT, strength VARCHAR(50), pack_size INTEGER, mrp DECIMAL(10,2), is_prescription_required BOOLEAN DEFAULT false, is_chronic BOOLEAN DEFAULT false, is_controlled BOOLEAN DEFAULT false, alternative_ids UUID[], created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS store_inventory (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), store_id UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE, medicine_id UUID NOT NULL REFERENCES medicines(id), quantity_in_stock INTEGER DEFAULT 0, reorder_level INTEGER DEFAULT 10, selling_price DECIMAL(10,2), discount_percent DECIMAL(5,2) DEFAULT 0.00, batch_number VARCHAR(50), expiry_date DATE, is_available BOOLEAN DEFAULT true, updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), UNIQUE(store_id, medicine_id, batch_number))`);

    await client.query(`CREATE TABLE IF NOT EXISTS prescriptions (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), patient_id UUID NOT NULL REFERENCES patients(id) ON DELETE CASCADE, store_id UUID REFERENCES stores(id), image_url TEXT NOT NULL, doctor_name VARCHAR(200), hospital_name VARCHAR(200), prescription_date DATE, diagnosis TEXT, ocr_status VARCHAR(20) DEFAULT 'pending', ocr_confidence DECIMAL(5,2), ocr_raw_text TEXT, verified_by UUID REFERENCES stores(id), verified_at TIMESTAMP WITH TIME ZONE, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS prescription_items (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), prescription_id UUID NOT NULL REFERENCES prescriptions(id) ON DELETE CASCADE, medicine_id UUID REFERENCES medicines(id), medicine_name_raw VARCHAR(300), dosage VARCHAR(100), frequency VARCHAR(100), duration_days INTEGER, quantity INTEGER, instructions TEXT, start_date DATE, estimated_end_date DATE, next_refill_date DATE, is_active BOOLEAN DEFAULT true, is_chronic BOOLEAN DEFAULT false, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS orders (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), order_number VARCHAR(20) UNIQUE NOT NULL, store_id UUID NOT NULL REFERENCES stores(id), patient_id UUID NOT NULL REFERENCES patients(id), prescription_id UUID REFERENCES prescriptions(id), order_type VARCHAR(20) DEFAULT 'regular', subtotal DECIMAL(10,2) NOT NULL DEFAULT 0.00, discount_amount DECIMAL(10,2) DEFAULT 0.00, delivery_charge DECIMAL(10,2) DEFAULT 0.00, total_amount DECIMAL(10,2) NOT NULL DEFAULT 0.00, delivery_type VARCHAR(20) DEFAULT 'delivery', delivery_address TEXT, delivery_notes TEXT, estimated_delivery_at TIMESTAMP WITH TIME ZONE, delivered_at TIMESTAMP WITH TIME ZONE, status VARCHAR(30) DEFAULT 'placed', payment_status VARCHAR(20) DEFAULT 'pending', payment_method VARCHAR(30), payment_id VARCHAR(100), confirmed_at TIMESTAMP WITH TIME ZONE, cancelled_at TIMESTAMP WITH TIME ZONE, cancellation_reason TEXT, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS order_items (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), order_id UUID NOT NULL REFERENCES orders(id) ON DELETE CASCADE, medicine_id UUID REFERENCES medicines(id), prescription_item_id UUID REFERENCES prescription_items(id), medicine_name VARCHAR(300) NOT NULL, quantity INTEGER NOT NULL DEFAULT 1, unit_price DECIMAL(10,2) NOT NULL, discount_percent DECIMAL(5,2) DEFAULT 0.00, total_price DECIMAL(10,2) NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS refill_reminders (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), store_id UUID NOT NULL REFERENCES stores(id), patient_id UUID NOT NULL REFERENCES patients(id), prescription_item_id UUID REFERENCES prescription_items(id), medicine_name VARCHAR(300) NOT NULL, refill_due_date DATE NOT NULL, escalation_level INTEGER DEFAULT 0, push_sent_at TIMESTAMP WITH TIME ZONE, whatsapp_sent_at TIMESTAMP WITH TIME ZONE, sms_sent_at TIMESTAMP WITH TIME ZONE, store_notified_at TIMESTAMP WITH TIME ZONE, patient_response VARCHAR(30), responded_at TIMESTAMP WITH TIME ZONE, resulting_order_id UUID REFERENCES orders(id), estimated_order_value DECIMAL(10,2), status VARCHAR(20) DEFAULT 'scheduled', created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS notifications (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), recipient_type VARCHAR(10) NOT NULL, recipient_id UUID NOT NULL, title VARCHAR(300) NOT NULL, body TEXT NOT NULL, channel VARCHAR(20) NOT NULL, reference_type VARCHAR(30), reference_id UUID, status VARCHAR(20) DEFAULT 'pending', sent_at TIMESTAMP WITH TIME ZONE, delivered_at TIMESTAMP WITH TIME ZONE, read_at TIMESTAMP WITH TIME ZONE, whatsapp_message_id VARCHAR(100), created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS store_daily_stats (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), store_id UUID NOT NULL REFERENCES stores(id), date DATE NOT NULL, total_orders INTEGER DEFAULT 0, total_revenue DECIMAL(12,2) DEFAULT 0.00, refill_orders INTEGER DEFAULT 0, new_patients INTEGER DEFAULT 0, active_patients INTEGER DEFAULT 0, reminders_sent INTEGER DEFAULT 0, reminders_converted INTEGER DEFAULT 0, revenue_recovered DECIMAL(12,2) DEFAULT 0.00, low_stock_items INTEGER DEFAULT 0, expiring_items INTEGER DEFAULT 0, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(), UNIQUE(store_id, date))`);

    await client.query(`CREATE TABLE IF NOT EXISTS otp_codes (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), phone VARCHAR(15) NOT NULL, code VARCHAR(6) NOT NULL, purpose VARCHAR(20) DEFAULT 'login', is_used BOOLEAN DEFAULT false, expires_at TIMESTAMP WITH TIME ZONE NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW())`);

    await client.query(`CREATE TABLE IF NOT EXISTS admin_users (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), name VARCHAR(150) NOT NULL, phone VARCHAR(15) NOT NULL UNIQUE, email VARCHAR(255), password_hash VARCHAR(255) NOT NULL, role VARCHAR(20) DEFAULT 'superadmin', is_active BOOLEAN DEFAULT true, created_at TIMESTAMP DEFAULT NOW())`);

    console.log('All tables created!');
    const sc = await client.query('SELECT count(*) FROM stores');
    if (parseInt(sc.rows[0].count) === 0) {
      console.log('Seeding demo data...');
      await seedDemoData(client);
      console.log('Demo data seeded!');
    } else { console.log('Data exists, skipping seed.'); }
  } catch (err) {
    console.error('Migration error:', err.message);
  } finally { client.release(); }
}

async function seedDemoData(client) {
  await client.query(`INSERT INTO stores (id,name,slug,owner_name,phone,email,password_hash,tagline,address,city,state,pincode,license_number,gst_number,delivery_radius_km,delivery_charge,min_order_amount,is_delivery_enabled,is_pickup_enabled,operating_hours,plan,is_active,is_verified) VALUES ('a1b2c3d4-e5f6-7890-abcd-ef1234567890','Apollo Pharmacy','apollo-pharmacy','Ramesh Sharma','9876543200','ramesh@apollopharmacy.in','\$2a\$10\$8K1p/a0dL1LXMIgoEDFrwOfMQkHzU/6P4S7Vm5rFd3x4RKQIK9V2C','Your Trusted Neighbourhood Pharmacy','Shop 12, Banjara Hills Main Road, Hyderabad','Hyderabad','Telangana','500034','TS-DRUG-2024-001','36AABCU9603R1ZM',5.0,50.00,200.00,true,true,'{"monday":{"open":"09:00","close":"22:00"},"tuesday":{"open":"09:00","close":"22:00"},"wednesday":{"open":"09:00","close":"22:00"},"thursday":{"open":"09:00","close":"22:00"},"friday":{"open":"09:00","close":"22:00"},"saturday":{"open":"09:00","close":"22:00"},"sunday":{"open":"10:00","close":"21:00"}}','pro',true,true) ON CONFLICT (phone) DO NOTHING`);

  await client.query(`INSERT INTO patients (id,phone,name,email,date_of_birth,gender,blood_group,allergies,conditions,address,city,pincode,is_active) VALUES ('p001-0000-0000-0000-000000000001','9876543210','Rajesh Kumar','rajesh@email.com','1975-06-15','male','B+',ARRAY['Penicillin'],ARRAY['Diabetes','Hypertension'],'Flat 203, Banjara Hills','Hyderabad','500034',true),('p002-0000-0000-0000-000000000002','9876543211','Priya Sharma','priya@email.com','1988-03-22','female','O+',ARRAY[]::TEXT[],ARRAY['Thyroid'],'House 45, Jubilee Hills','Hyderabad','500033',true),('p003-0000-0000-0000-000000000003','9876543212','Amit Patel','amit@email.com','1965-11-08','male','A+',ARRAY['Sulfa drugs'],ARRAY['Asthma','Diabetes'],'12B, Madhapur','Hyderabad','500081',true),('p004-0000-0000-0000-000000000004','9876543213','Sunita Devi','sunita@email.com','1970-07-30','female','AB+',ARRAY[]::TEXT[],ARRAY['Cholesterol'],'Plot 89, Kukatpally','Hyderabad','500072',true),('p005-0000-0000-0000-000000000005','9876543214','Vikram Singh','vikram@email.com','1982-01-14','male','B-',ARRAY['Aspirin'],ARRAY['Cardiac'],'Flat 67, Kondapur','Hyderabad','500084',true) ON CONFLICT (phone) DO NOTHING`);

  await client.query(`INSERT INTO store_patients (id,store_id,patient_id,loyalty_points,lifetime_value,total_orders,adherence_score,risk_level,last_order_at,tags,is_active) VALUES ('sp001-0000-0000-0000-000000000001','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p001-0000-0000-0000-000000000001',250,45680.00,28,92,'normal',NOW()-INTERVAL '2 days',ARRAY['diabetic','high-value'],true),('sp002-0000-0000-0000-000000000002','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p002-0000-0000-0000-000000000002',120,23450.00,15,78,'normal',NOW()-INTERVAL '5 days',ARRAY['thyroid'],true),('sp003-0000-0000-0000-000000000003','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p003-0000-0000-0000-000000000003',50,8900.00,8,45,'at_risk',NOW()-INTERVAL '25 days',ARRAY['diabetic'],true),('sp004-0000-0000-0000-000000000004','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p004-0000-0000-0000-000000000004',180,34200.00,22,88,'normal',NOW()-INTERVAL '3 days',ARRAY['cholesterol'],true),('sp005-0000-0000-0000-000000000005','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p005-0000-0000-0000-000000000005',80,15600.00,12,62,'at_risk',NOW()-INTERVAL '15 days',ARRAY['cardiac'],true) ON CONFLICT DO NOTHING`);

  await client.query(`INSERT INTO medicines (name,generic_name,manufacturer,category,strength,pack_size,mrp,is_prescription_required,is_chronic) VALUES ('Metformin 500mg','Metformin Hydrochloride','USV Limited','tablet','500mg',60,85.00,true,true),('Amlodipine 5mg','Amlodipine Besylate','Cipla','tablet','5mg',30,45.00,true,true),('Atorvastatin 10mg','Atorvastatin Calcium','Sun Pharma','tablet','10mg',30,120.00,true,true),('Thyronorm 50mcg','Levothyroxine Sodium','Abbott','tablet','50mcg',120,135.00,true,true),('Telmisartan 40mg','Telmisartan','Glenmark','tablet','40mg',30,95.00,true,true),('Pantoprazole 40mg','Pantoprazole Sodium','Alkem','tablet','40mg',15,65.00,true,false),('Azithromycin 500mg','Azithromycin','Cipla','tablet','500mg',3,75.00,true,false),('Paracetamol 500mg','Paracetamol','GSK','tablet','500mg',10,15.00,false,false),('Cetirizine 10mg','Cetirizine HCl','Mankind','tablet','10mg',10,25.00,false,false),('Amoxicillin 500mg','Amoxicillin','Cipla','capsule','500mg',10,55.00,true,false),('Dolo 650','Paracetamol','Micro Labs','tablet','650mg',15,30.00,false,false),('Vitamin D3 60K','Cholecalciferol','Abbott','capsule','60000IU',4,120.00,false,false),('B-Complex Forte','Vitamin B Complex','Abbott','tablet','-',30,35.00,false,false) ON CONFLICT DO NOTHING`);

  await client.query(`INSERT INTO store_inventory (id,store_id,medicine_id,quantity_in_stock,reorder_level,selling_price,discount_percent,batch_number,expiry_date,is_available) SELECT uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890',m.id,CASE WHEN random()>0.3 THEN (random()*200+20)::int ELSE (random()*10+1)::int END,15,m.mrp*(1-(random()*0.15)),(random()*15)::int,'BATCH-'||(random()*9000+1000)::int,NOW()+INTERVAL '1 year'*(random()*2+0.1),true FROM medicines m ON CONFLICT DO NOTHING`);

  await client.query(`INSERT INTO orders (id,order_number,store_id,patient_id,order_type,subtotal,discount_amount,delivery_charge,total_amount,delivery_type,delivery_address,status,payment_status,payment_method,created_at) VALUES ('ord001-0000-0000-0000-000000000001','RX-2026-00001','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p001-0000-0000-0000-000000000001','regular',420.00,0,50.00,470.00,'delivery','Flat 203, Banjara Hills','delivered','paid','cod',NOW()-INTERVAL '2 days'),('ord002-0000-0000-0000-000000000002','RX-2026-00002','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p002-0000-0000-0000-000000000002','refill',270.00,0,0,270.00,'pickup',NULL,'delivered','paid','upi',NOW()-INTERVAL '5 days'),('ord003-0000-0000-0000-000000000003','RX-2026-00003','a1b2c3d4-e5f6-7890-abcd-ef1234567890','p001-0000-0000-0000-000000000001','prescription',890.00,50.00,50.00,890.00,'delivery','Flat 203, Banjara Hills','ready','pending','cod',NOW()-INTERVAL '1 day') ON CONFLICT DO NOTHING`);

  await client.query(`INSERT INTO admin_users (id,name,phone,email,password_hash,role) VALUES ('admin-0000-0000-0000-000000000001','RxMax Admin','9999999999','admin@rxmax.in','\$2a\$10\$8K1p/a0dL1LXMIgoEDFrwOfMQkHzU/6P4S7Vm5rFd3x4RKQIK9V2C','superadmin') ON CONFLICT (phone) DO NOTHING`);

  await client.query(`INSERT INTO store_daily_stats (id,store_id,date,total_orders,total_revenue,refill_orders,new_patients,active_patients,reminders_sent,reminders_converted,revenue_recovered) SELECT uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890',d::date,(random()*12+5)::int,(random()*5000+6000)::numeric(10,2),(random()*5+2)::int,(random()*3)::int,(random()*20+10)::int,(random()*8+2)::int,(random()*5+1)::int,(random()*2000+500)::numeric(10,2) FROM generate_series(NOW()-INTERVAL '30 days',NOW(),INTERVAL '1 day') AS d ON CONFLICT DO NOTHING`);
  const existingRefills = await client.query(`SELECT count(*) FROM refill_reminders WHERE store_id = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890'`);
  if (parseInt(existingRefills.rows[0].count) === 0) {
    await client.query(`INSERT INTO refill_reminders (id,store_id,patient_id,medicine_name,refill_due_date,escalation_level,status,estimated_order_value) VALUES (uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p001-0000-0000-0000-000000000001','Metformin 500mg',(NOW()+INTERVAL '2 days')::date,0,'scheduled',85.00),(uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p001-0000-0000-0000-000000000001','Amlodipine 5mg',(NOW()+INTERVAL '5 days')::date,0,'scheduled',45.00),(uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p002-0000-0000-0000-000000000002','Thyronorm 50mcg',(NOW()+INTERVAL '8 days')::date,0,'scheduled',135.00),(uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p003-0000-0000-0000-000000000003','Atorvastatin 10mg',(NOW()-INTERVAL '1 day')::date,1,'sent',120.00),(uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p004-0000-0000-0000-000000000004','Telmisartan 40mg',(NOW()+INTERVAL '12 days')::date,0,'scheduled',95.00),(uuid_generate_v4(),'a1b2c3d4-e5f6-7890-abcd-ef1234567890','p005-0000-0000-0000-000000000005','Pantoprazole 40mg',(NOW()+INTERVAL '3 days')::date,0,'scheduled',65.00) ON CONFLICT DO NOTHING`);
  }
}

module.exports = { runMigrations };
